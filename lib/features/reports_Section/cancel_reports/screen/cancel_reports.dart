import 'package:flutter/material.dart';

class CancelReportsScreen extends StatelessWidget {
  const CancelReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("cancel Page"),
      ),
    );
  }
}
